/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["unsplash.com", "img.freepik.com", "cdn.pixabay.com"],
  },
};

module.exports = nextConfig;
